
{% extends "GalleryBundle::layout.html.twig" %}

{% block navigation %}
    <ul class="nav">
        {% for event in events %}
            <li {% if currentEvent == event %}class="active"{% endif %}>
                <a href="{{ path('micha149_gallery_gallery_event', {'event': event}) }}">{{ event }}</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}

{% block content %}

    <h1>Das ist ein Test</h1>
    
    {% if files %}
        <div class="thumbnails">     
            {% for file in files %}
                {% if loop.index%4 == 0%}
                    <div class="row">
                {% endif %}
                <div class="span3">
                    <a href="{{baseUrl}}{{file.lightbox}}" class="thumbnail" data-fancybox-group="gallery" data-download-url="{{baseUrl}}{{file.original}}">
                        <img src="{{baseUrl}}{{file.thumbnail}}" alt="{{file.filename}}">
                    </a>
                </div>
                {% if loop.index%4 == 0%}
                    </div>
                {% endif %}                
            {% endfor %}    
        </div>
    {% endif %}
    
    <script>
        $(function () {
    		$('.thumbnail').fancybox({
    			closeBtn  : false,
                arrows: false,
    			helpers : {
    				buttons	: {}
    			}
    		});
		});
    </script>
    
{% endblock %}